version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: avantix-one-db
    restart: always
    environment:
      POSTGRES_DB: planillas_tom_tom_wok
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 4a11a20ef54b01f798f7
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: avantix-one-backend
    restart: always
    depends_on:
      - db
    environment:
      DATABASE_URL: postgres://avaone:ea167ca0e95c175c1051@avantix_one_db_planillas_avantixone:5432/db_planillas_avantixone?sslmode=disable
      PORT: 3000
    ports:
      - "3000:3000"

  frontend:
    build: .
    container_name: avantix-one-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
